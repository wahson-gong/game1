# 小兔兔接食物 — 玩法与移植说明

本文记录当前 H5 版本的功能、规则和实现要点，方便以后转写到 iOS、Android、微信小游戏或其他引擎（SpriteKit、Unity、Cocos、微信小游戏 API 等）。

- 当前实现：纯前端，无服务端。H5 代码在 `h5/`：`index.html` + `css/style.css` + `js/game.js`
- 一局时长：60 秒
- 目标：左右移动、跳跃接住从天上掉下来的食物，尽量拿高分

---

## 1. 游戏一句话

控制地面上的小兔子，接住下落的食物。好食物加分，巧克力蛋糕扣分并短暂晕眩。漏接的香蕉会变成香蕉皮，踩到会滑行。时间结束比总分，并记住历史最高分。

---

## 2. 界面与流程

### 2.1 画面结构（从上到下）

| 层 | 内容 |
| --- | --- |
| 背景 | 蓝天渐变、太阳、三朵循环飘动的云 |
| 远景 | 绿色山丘 |
| 地面 | 草地条，兔子站在这条上面 |
| 玩法层 | 下落食物、地上的香蕉皮、得分飘字、兔子 |
| HUD | 左上：总分 + 五种食物明细；右上：倒计时、音乐开关、暂停（开局后才显示） |
| 遮罩 | 开始 / 暂停 / 结束 三张毛玻璃弹层 |

兔子、食物、HUD 都是矢量 CSS 绘制，没有图片资源。移植时可继续用矢量，或换成切图 / Spine。

### 2.2 状态机

```
开始页  --点「开始游戏」-->  进行中
进行中  --点「暂停」------>  暂停
暂停    --点「继续游戏」-->  进行中
进行中  --倒计时到 0----->  结算页
结算页  --点「再玩一次」-->  进行中（直接重开，不再回开始页）
```

- 未开局：只显示开始页；暂停按钮隐藏。
- 进行中：开始页 / 结束页隐藏；暂停按钮显示为「暂停」。
- 暂停：世界冻结（掉落、移动、生成、倒计时都停）；云的动画也停；BGM 音量降低；弹出「已暂停」。
- 结算：隐藏暂停按钮；BGM 音量降低；显示本局分和历史最好。

开局后约 280ms 内忽略跳跃，避免「开始」按钮的点击穿透把兔子弹起来。

---

## 3. 怎么玩

### 3.1 目标

60 秒内尽可能多接食物。总分不能低于 0（扣到 0 为止）。没接到的食物落到地面附近后消失，不扣分。**香蕉例外**：漏接后变成香蕉皮，留在地上 3 秒。

### 3.2 操作

| 设备 | 左右移动 | 跳跃 |
| --- | --- | --- |
| 桌面鼠标 | 鼠标在窗口内移动，兔子跟随指针水平位置 | 单击游戏画面（左键） |
| 触屏 | 按住拖动 | 点屏幕 |
| 键盘（附加） | `←` `→` 或 `A` `D` | 无（当前没有空格跳） |

- HUD 按钮、弹层上的点击不触发跳跃。
- 右键不跳。
- 跳跃上升、下落时，左右跟随仍然有效，可以在空中平移去接食物。
- **滑行中**：不能跟鼠标 / 触摸 / 键盘左右移动，但可以跳跃（含连跳）。
- 被蛋糕击晕时：不能移动、不能跳；若正在滑行会立刻停下。

### 3.3 跳跃规则

- 点一下跳一次。空中再点可以接力往上跳。
- **同一段滞空最多跳 3 次**（含第一次从地面起跳）。第 4 次点击无效，但左右移动仍可用。
- 落地后次数清零，可以再跳 3 次。
- 每次起跳都会把向上速度重置为同一初速度（不是叠加，是刷新）。
- 击晕时立刻落地并清零跳跃次数。

---

## 4. 食物与得分

五种食物，按权重随机生成（权重合计 100，等价于百分比）。

| 食物 | 类型 id | 分数 | 生成权重 | 下落档位 fall | 接到后的表现 |
| --- | --- | --- | --- | --- | --- |
| 香蕉 | `banana` | +6 | 14 | 1（最快） | 开心脸 |
| 草莓 | `strawberry` | +4 | 16 | 2 | 大笑跳跃动画 + 笑声 |
| 青菜 | `veggie` | +2 | 22 | 1（最快） | 开心脸 |
| 胡萝卜 | `carrot` | +1 | 36 | 3 | 开心脸 |
| 巧克力蛋糕 | `cake` | −3 | 12 | 5（最慢） | 先摇头，再晕倒约 2 秒 |

说明：

- `fall` 是 1–5 的档位，**数字越小掉得越快**。实际速度见第 5 节。
- HUD 明细按「个数 + 该种类累计得分」展示，例如接 2 个草莓显示 `2个`、`+8分`。蛋糕累计为负数，用红色。
- 接到瞬间在食物位置冒 `+6` / `-3` 这类飘字，约 0.8 秒后消失。
- 漏接：胡萝卜 / 青菜 / 草莓 / 蛋糕从画面底部消失，不改分数、不改明细。
- 漏接香蕉：不改分数，但在落点生成香蕉皮。香蕉皮不是食物，不能加分。

### 4.1 蛋糕击晕（必须复刻）

1. 立即落地，速度清零，不能再跟指针。
2. **摇头 0.42 秒**。
3. 接着 **晕倒 2 秒**：身体倒下、眼睛变成一线、头上转星星。
4. 这两段期间不能移动、不能跳、不能再接食物（食物会继续掉，可能漏接）。
5. 晕眩结束后恢复站立，可以继续玩。

总晕眩时长 = `0.42 + 2 = 2.42` 秒。

击晕会立刻清掉滑行。

### 4.2 香蕉皮与滑行（必须复刻）

1. 香蕉到达漏接线（画面高度 × 92%）且没被接住时，在当前水平位置生成香蕉皮。
2. 香蕉皮贴在草地上，寿命 **3 秒**，最后约 0.35 秒淡出后删除。暂停时寿命不走。
3. 兔子**靠近地面**（`rabbitY < 14`）且脚部盒与香蕉皮重叠，视为踩到。空中跳过香蕉皮不触发。
4. 踩到后香蕉皮立刻消失，兔子开始滑行：剩余滑行距离 `+= 230 px`。
5. **叠加**：已经在滑时再踩到另一块皮，只加距离，不改速度。方向保持当前滑行方向。
6. 第一次起滑的方向：当前水平速度符号 → 否则指向 `targetX` 的方向 → 否则沿用上一次方向（默认向右）。
7. 滑行速度恒为 `440 px/s`。碰到左右墙会**掉头**，剩余距离继续花完。
8. 滑行中：
   - 不更新 `targetX`（鼠标、触摸、键盘都无效）。
   - 仍可跳跃、仍可接食物。
   - 空中也继续按滑行速度横移（惯性），落地后若还在滑就接着滑。
9. 滑行结束：把 `targetX` 设为当前 `x`，速度清零，避免弹向旧的鼠标位置。
10. 开局、击晕、结束清场时清掉所有香蕉皮和滑行状态。

---

## 5. 数值与物理（移植时按同一套公式）

坐标系：原点在游戏区域左上。兔子用「距底边的高度」`rabbitY`（地面为 0，向上为正）。食物用距顶边的 `y`（向下增大）。

当前 H5 按 **像素 / 秒** 计算。移植到不同分辨率时，建议改成相对屏幕高度的单位，或按设计稿（例如 390×844）做等比缩放。下面是 H5 现状。

### 5.1 尺寸

| 量 | 值 |
| --- | --- |
| 兔子逻辑宽 | 92 px（高约 110） |
| 食物碰撞宽×高 | 54 × 62 |
| 香蕉皮宽×高 | 56 × 28 |
| 地面：兔子脚底距容器底边 | 容器高度 × 7% |
| 食物漏接线 | 容器高度 × 92%（再往下删除；香蕉在此变成皮） |
| 香蕉皮纵坐标 | `画面高 - 地面高 - 20`（贴在草地上） |
| 跳跃最大高度 | 容器高度 × 58%（到顶把向上速度抹成 0） |

### 5.2 水平移动

- 指针目标：`targetX = 指针X - 兔子宽度/2`，再夹紧到 `[0, 画面宽 - 兔子宽]`。
- 键盘额外以 520 px/s 推动 `targetX`。
- 兔子不瞬移，用**临界阻尼弹簧**追 `targetX`：

```
response = 空中 ? 0.09 : 0.14
omega    = 2 / response
accel    = omega² * (targetX - x) - 2 * omega * vx
vx      += accel * dt
x       += vx * dt
```

- 撞到左右边界时，把 `vx` 乘 0.15（轻微回弹，不是硬停）。
- 空中的 `response` 更小，左右跟手更快，方便空中接食。
- **滑行时不用弹簧**，改走 5.7 节的匀速滑行。

### 5.3 跳跃

| 常量 | 值 | 含义 |
| --- | --- | --- |
| `JUMP_VELOCITY` | 560 | 每次起跳的向上初速度（px/s） |
| `GRAVITY` | 1500 | 重力加速度（px/s²） |
| `MAX_AIR_JUMPS` | 3 | 一段滞空最多跳几次 |

每帧（未击晕时）：

```
vy -= GRAVITY * dt
y  += vy * dt
若 y <= 0：落地，y=0，vy=0，跳跃次数=0
若 y > 画面高 * 0.58：夹紧，且若仍在上升则 vy=0
```

单次起跳理论最大高度 ≈ `v² / (2g) = 560² / 3000 ≈ 105 px`。三次接力可跳得更高，但不会超过屏幕 58% 高。

### 5.4 生成与下落

- 开局 `spawnAcc = 0.8`，所以第一件食物会很快出现。
- 生成间隔（秒）：

```
spawnEvery = max(0.45, 1.15 - 已经过秒数 * 0.012)
```

  开局约 1.15 秒一件，越到后面越快，最快 0.45 秒一件。

- 水平出生点：`28 + random * (画面宽 - 56)`，避免贴边。
- 初始 `y = -70`（在屏幕上方外）。
- 下落速度（px/s）：

```
speed = (360 / fall) * (1 + 已经过秒数 * 0.008)
```

  香蕉、青菜 `fall=1` 约 360 px/s 起；蛋糕 `fall=5` 约 72 px/s 起。整局速度会慢慢加快。

- 轻微左右摆动（视觉，也参与碰撞 X）：

```
swayX = x + sin(timeMs / 400 + phase) * sway * 0.08
sway  = 16 ~ 26（随机）
```

### 5.5 碰撞

用兔子的实际屏幕矩形做接住盒，再内缩一点：

- 左右各收 8% 宽度
- 顶边向上扩 8 px
- 底边向上收 6% 高度

食物盒：中心在当前 `swayX`，宽 54、高 62。AABB 重叠即接住。

击晕期间不做接住判定。

踩香蕉皮用**脚部盒**（与接住盒不同）：

- 左右各收 16% 宽度
- 只取兔子底部 24% 高度

香蕉皮盒：中心在生成点 `x`，宽 56、高 28。AABB 重叠且 `rabbitY < 14` 即踩到，皮立刻删除。

### 5.6 时间

- 独立 1 秒心跳倒计时，暂停时不减。
- 主循环 `dt` 上限 0.05 秒，避免切后台回来后一帧跳太远。
- 暂停恢复时重设 `lastTs`，防止一帧巨大 `dt`。

### 5.7 滑行

| 常量 | 值 | 含义 |
| --- | --- | --- |
| `PEEL_LIFE` | 3 | 香蕉皮存在秒数 |
| `SLIDE_SPEED` | 440 | 滑行速度（px/s） |
| `SLIDE_DISTANCE` | 230 | 每踩一块皮增加的滑行距离（px） |

每帧（未击晕且正在滑行）：

```
step = 440 * dt
x   += slideDir * step
若碰到左右墙：slideDir *= -1，并把 x 夹紧
slideRemain -= step
若 slideRemain <= 0：结束滑行，targetX = x，vx = 0
```

同一帧仍做跳跃积分。滑行与跳跃互不取消。

---

## 6. HUD 与文案

左上：

- 得分
- 明细顺序：香蕉 → 草莓 → 青菜 → 胡萝卜 → 巧克力蛋糕  
  每行：小图标、名字、`N个`、带符号的分数

右上：

- 时间（整数秒）
- 「🎵 音乐开」/「🔇 音乐关」
- 开局后：「暂停」/ 暂停中变成「继续」

开始页要点文案：

- 标题：小兔兔接食物
- 说明：左右移动小兔兔，接住天上掉下来的好吃的！
- 规则列表见第 4 节分数
- 提示：点击屏幕可以跳起来接食物，连点最多跳 3 次；空中也能跟着鼠标左右移动。漏接的香蕉会变成香蕉皮，踩到会滑出去，连踩会滑得更远
- 按钮：开始游戏

暂停页：已暂停 / 点继续，小兔兔就会接着接食物 / 继续游戏

结算页：时间到啦 / 本局分数 / 历史最好 / 再玩一次

---

## 7. 表现与动效

移植时建议保留这些“手感”，不必 1:1 抄 CSS。

**兔子**

- 地面跟随：弹簧，不要线性插值到目标。
- 上升：身体略拉长（`scale(0.96, 1.06)`）。
- 下落：身体略压扁（`scale(1.05, 0.94)`）。
- 空中影子变小、变淡。
- 开心：嘴角上扬，约 0.45 秒。
- 接到草莓：夸张跳一下 + 脸红，约 0.65 秒，同时播笑声。
- 蛋糕：摇头 0.42 秒 → 侧躺晕倒 2 秒。
- 滑行：身体向滑行方向倾斜，影子拉长；可与上升 / 下落形变叠在一起。

**世界**

- 香蕉落地变皮：短促压扁落地（约 0.34 秒）。
- 香蕉皮最后 0.35 秒淡出。

- 云从右往左循环漂。
- 暂停时云停。
- HUD、弹层：毛玻璃、大圆角、按压缩放 0.97。
- 配色是草地晴天，不要用常见的紫蓝 AI 渐变。
- 系统字体：SF Pro / PingFang / system-ui。

**无障碍**

- 支持 `prefers-reduced-transparency`：去掉模糊，改实色。
- 支持 `prefers-reduced-motion`：关掉云、夸张跳跃动画和香蕉皮落地动画。

---

## 8. 声音

当前没有音频文件，全部用振荡器实时合成。

### 8.1 背景音乐

- 默认开。开关写入本地：键名 `rabbit-food-music-on`，`"0"` 为关，其它为开。
- 8 秒循环。旋律用三角波，低音用正弦波。
- 进行中主音量约 0.18；暂停 / 结束降到约 0.08。
- 需要用户手势后才能出声（Web 限制）。原生 App 无此限制，但微信小游戏仍需遵守用户点击后播音频的策略。

旋律（时间秒, 频率 Hz, 时值秒）：

```
0.0  523.25  0.28
0.5  659.25  0.28
1.0  783.99  0.28
1.5  659.25  0.28
2.0  587.33  0.28
2.5  698.46  0.28
3.0  783.99  0.55
4.0  659.25  0.28
4.5  783.99  0.28
5.0  880.00  0.28
5.5  783.99  0.28
6.0  659.25  0.28
6.5  587.33  0.28
7.0  523.25  0.70
```

低音：

```
0  130.81  0.9
2  174.61  0.9
4  196.00  0.9
6  130.81  0.9
```

### 8.2 笑声（仅草莓）

四声短音，主频三角波 + 一半频率的正弦波：

```
0.00s  620 Hz  0.09s
0.11s  780 Hz  0.08s
0.21s  860 Hz  0.10s
0.34s  720 Hz  0.14s
```

移植时可做成两段音频：`bgm.loop` + `laugh.mp3`，逻辑不变。

---

## 9. 本地存档

| 键 | 含义 |
| --- | --- |
| `rabbit-food-best-score` | 历史最高分（整数） |
| `rabbit-food-music-on` | `"0"` 关音乐，否则开 |

无账号、无排行榜、无内购。结算时：`best = max(本局分, 已存最好)`。

iOS / Android 可用 `UserDefaults` / `SharedPreferences`。微信小游戏用 `wx.setStorageSync` / `wx.getStorageSync`，键名可保持不变。

---

## 10. 当前工程结构

```
h5/                 当前可玩的 H5 版本
  index.html        页面、HUD、三个弹层、兔子 DOM
  css/style.css     场景、食物外形、兔子、毛玻璃 HUD
  js/game.js        全部玩法、物理、音频、存档
miniprogram/        微信小程序（待移植）
android/            Android（待移植）
ios/                iOS（待移植）
assets/             跨端共用资源（如小程序图标）
游戏设计说明.md      玩法与移植说明
```

无构建步骤。本地请在 `h5/` 目录启动静态服务器，例如：`python3 -m http.server 8765`。

---

## 11. 移植建议

逻辑（分数、生成、物理、击晕、三连跳、香蕉皮、滑行叠加）应跨平台保持一致。输入和渲染按平台替换。

### 11.1 共同注意点

1. **输入分离**：`targetX`（移动）和 `tryJump()`（跳跃）是两个接口。鼠标移动 / 触摸拖动只改 `targetX`；按下才跳。
2. **空中也能横向移动**：跳跃积分和水平弹簧每帧都跑，不要写成“落地才能左右”。滑行中除外：滑行时左右不受控，但跳跃仍可用。
3. **暂停要冻一切**：生成、掉落、香蕉皮寿命、移动、倒计时、动画。不要只冻画面。
4. **击晕要冻操作但世界还在掉食物**；击晕要清滑行。
5. **香蕉皮是独立对象**，不要和下落食物共用“接住就加分”的逻辑。
6. **滑行叠加的是距离不是速度**。同一块皮只能踩一次（踩到即删除）。
7. **总分下限 0**，明细仍按真实加减累计（蛋糕可以是负分）。
8. **分辨率**：H5 是全屏自适应。建议设计稿先定竖屏安全区（刘海、微信胶囊按钮）。食物大小、跳跃高度、滑行距离按屏幕高度比例缩放，否则平板上会显得跳得很矮、滑得很短。
9. **帧循环**：用 `dt`，不要写死 60fps。`dt` 要封顶。
10. **随机**：生成用权重抽表，不要五种平均随机。

### 11.2 iOS

- 可用 SpriteKit / SwiftUI Canvas / Unity。
- 触摸：`touchesMoved` 更新 `targetX`，`touchesBegan`（点在非按钮区域）调用跳跃。
- 最高分：`UserDefaults`。
- 音频：导出 wav/aac，用 `AVAudioPlayer` 循环 BGM；草莓用短音效。需处理静音键和后台暂停。
- 建议支持 Game Center 排行榜（当前没有，属扩展）。

### 11.3 Android

- 可用自定义 `SurfaceView` / Jetpack Compose / Unity。
- 触摸同 iOS。注意多点触控：第一个手指移动，点按算跳。
- 存档：`SharedPreferences` 或 DataStore。
- 生命周期：`onPause` 应进入游戏内暂停，避免后台继续倒计时。

### 11.4 微信小游戏

- 渲染：Canvas 2D 或微信游戏引擎 / Cocos。
- 输入：`wx.onTouchStart` 跳跃 + 设目标 X；`wx.onTouchMove` 只更新 X。
- 避开右上角胶囊按钮，HUD 不要放在那一块。
- 音频：`wx.createInnerAudioContext`，必须跟用户点击走；BGM `loop=true`。
- 存档：`wx.setStorageSync`。
- 分享（扩展，当前没有）：结算页可加「分享成绩」。
- 没有 DOM，食物和兔子都要自己画或用精灵图。
- 微信小游戏是竖屏为主，逻辑宽高建议固定设计分辨率再 letterbox。

### 11.5 建议的最小模块划分

移植时不要按 DOM 结构抄，按下面拆：

| 模块 | 职责 |
| --- | --- |
| `GameState` | running / paused、分数、倒计时、明细、击晕计时、滑行剩余距离 |
| `Rabbit` | x/y、速度、跳跃次数、弹簧跟随、落地、滑行方向 |
| `Spawner` | 权重抽食物、间隔、下落速度曲线 |
| `Items` | 位置、摆动、碰撞、漏接删除或变皮 |
| `Peels` | 香蕉皮位置、寿命、脚部碰撞 |
| `Input` | 指针/触摸/键盘 → targetX + jump（滑行中忽略 targetX） |
| `Audio` | BGM 开关、淡出、笑声 |
| `Save` | 最高分、音乐开关 |
| `UI` | 开始 / 暂停 / 结算 / HUD |

主循环伪代码：

```
if !running: return
if paused: 只继续申请下一帧
dt = min(0.05, 真实间隔)

if !stunned:
    若正在滑行：
        匀速横移，碰墙掉头，扣剩余距离
    否则：
        更新 targetX（指针 + 键盘）
        弹簧积分水平位置
    若在空中或有跳跃速度：重力积分，处理落地 / 限高
else:
    扣晕眩时间，到点恢复

按间隔生成食物
移动食物，做碰撞或漏接（香蕉漏接 → 生成香蕉皮）
扣香蕉皮寿命，到期删除
靠近地面时用脚部盒检测香蕉皮，踩到则叠加滑行
扣情绪动画计时
```

---

## 12. 明确没有的功能

写移植或加需求时避免误当成已有：

- 没有生命值、没有 combo
- 漏接不扣分
- 没有道具、没有关卡，只有 60 秒一局
- 没有联网排行榜、登录、广告、内购
- 没有暂停以外的设置页
- 键盘不能跳跃

---

## 13. 验收清单（任意平台）

- [ ] 开始、暂停、继续、结束再来一局流程正确
- [ ] 60 秒到点必结算；暂停时秒数不动
- [ ] 五种食物分数、权重、相对快慢正确
- [ ] 总分不低于 0；HUD 明细个数和分数同步
- [ ] 鼠标/触摸左右跟随，空中同样能横移并接住
- [ ] 点击起跳；连跳最多 3 次；落地重置
- [ ] 草莓：大笑动画 + 笑声
- [ ] 蛋糕：摇头 → 晕倒 2 秒，期间不能动、不能跳、不能接
- [ ] 音乐开关能记住；暂停时 BGM 变轻
- [ ] 最高分跨进程还在
- [ ] 按钮和弹层不会误触跳跃
- [ ] 漏接香蕉变成香蕉皮，3 秒后消失；其它食物漏接仍立刻消失
- [ ] 踩到香蕉皮会滑行；连踩叠加距离；滑行中可跳、不跟鼠标
- [ ] 滑行碰到左右墙会掉头；蛋糕击晕会打断滑行
